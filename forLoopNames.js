const classNames = ['Aa', 'Abelle','Bianca', 'Caezzy', 'Christian', 'Dominic', 'Edward', 
    'Ian', 'Jacob', 'Jerome', 'Jheric', 'Jim', 'John', 'Jomell', 'Kai']
 
for(let names = 0; names < classNames.length; names++){
    console.log(classNames[names])
    if (classNames[names] === 'Xai') {
        break
    }
  }